# Copy to .env for local examples (never commit real keys)
XBOARD_API_KEY=xbk_test_replace_me
XBOARD_BASE_URL=https://api.dev.example.com

# Optional example inputs (CRM customer must already exist)
# XBOARD_EXTERNAL_CUSTOMER_ID=CRM-1001
# XBOARD_BOARD_TYPE=shared
# XBOARD_POST_ID=
